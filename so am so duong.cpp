#include<stdio.h>
	int main(){
		int a;
		printf("Nhap vao mot so:\n");
		scanf("%d", &a);
		if (a>0) printf("Day la so duong:%d", a);
		else if (a==0) printf("Khong xac dinh:%d", a);
		else if (a<0) printf("Day la so am:%d", a);
		
		
		
		
		
		
		return 0;
		
		}
